program prog1; 
var <- int n; ->

int func fibonacci (var <- int num; ->)
var <- int res; ->
{
  if (num < 2) {
    res = num;
  } else {
    res = fibonacci(num - 1) + fibonacci(num - 2);
  }
  return res;
}

main() {
    print(fibonacci(10));
}
